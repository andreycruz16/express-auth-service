info:
  name: /verify-email
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:3000/auth/verify-email
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
