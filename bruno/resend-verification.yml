info:
  name: /resend-verification
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3000/auth/resend-verification
  body:
    type: json
    data: |-
      {
        "email": "markandreydelacruz@ymail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
