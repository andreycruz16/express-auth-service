info:
  name: /register
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3000/auth/register
  body:
    type: json
    data: |
      {
        "email": "markandreydelacruz@cognizant.com",
        "password": "mark123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
